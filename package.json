{
  "name": "trtw-tv",
  "version": "2.0.0",
  "description": "Subtítulos en español en directo para Twitch y YouTube (Whisper + traducción local)",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "prepare": "node scripts/build.mjs",
    "test": "node --test \"test/*.test.mjs\"",
    "package": "npm run build && cd extension && zip -r ../trtw-tv.zip . -x \"*.map\""
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "@huggingface/transformers": "~3.8.1"
  },
  "type": "module"
}
